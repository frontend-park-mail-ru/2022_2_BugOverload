import template from '@components/Genre/genre.handlebars';
import { genres } from '@assets/icons/genre/genres.js';

export class Genre {
    static createGenre {
        // Film.decoreFilmInfo(filmData);

        const genres = template({

        });

        return genres;
    }
}